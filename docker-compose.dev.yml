version: '3'

services:
  db:
    image: postgres:12
    environment:
      POSTGRES_DB: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - '2345:5432'
      # command: ["postgres", "-c", "log_statement=all"]
