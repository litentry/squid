{
  "name": "khala-identity",
  "private": true,
  "scripts": {
    "start:khala": "make down && make up && make build && make migrate && make processKhala",
    "start:polkadot": "make down && make up && make build && make migrate && make processPolkadot",
    "processor:khala:start": "node lib/processors/khalaProcessor.js",
    "processor:polkadot:start": "node lib/processors/polkadotProcessor.js",
    "build": "rm -rf lib && tsc",
    "query-node:start": "squid-graphql-server"
  },
  "dependencies": {
    "@polkadot/api": "^6.11.1",
    "@polkadot/types": "^6.11.1",
    "@subsquid/graphql-server": "^0.1.2",
    "@subsquid/ss58": "^0.0.2",
    "@subsquid/substrate-processor": "^0.2.3",
    "bignumber.js": "^9.0.2",
    "dotenv": "^10.0.0",
    "pg": "^8.7.1",
    "typeorm": "^0.2.41"
  },
  "devDependencies": {
    "@subsquid/cli": "^0.1.0",
    "@subsquid/substrate-metadata-explorer": "^0.0.6",
    "@subsquid/substrate-typegen": "^0.1.0",
    "@types/node": "^16.11.17",
    "@typescript-eslint/eslint-plugin": "^5.10.0",
    "eslint": "7.26.0",
    "prettier": "^2.3.2",
    "typescript": "~4.5.4"
  }
}
