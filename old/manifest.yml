version: '>=5'
description: Litentry Subsquid manifest

typegen:
  metadata:
    source: wss://khala.api.onfinality.io/public-ws
  outDir: src/types
  events:
    - balances.Transfer
  calls:

mappings:
  mappingsModule: lib/mappings
  eventHandlers:
    - event: balances.Transfer
      handler: handleTransfer
  extrinsicHandlers:
  preBlockHooks:
  postBlockHooks:
