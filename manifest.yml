version: '>=5'
description: Litentry Subsquid manifest

typegen:
  metadata:
    source: wss://rpc.polkadot.io
  outDir: src/types
  events:
    - identity.IdentitySet
  calls:

mappings:
  mappingsModule: lib/mappings
  eventHandlers:
    - event: identity.IdentitySet
      handler: handleIdentitySet
  extrinsicHandlers:
  preBlockHooks:
  postBlockHooks:
